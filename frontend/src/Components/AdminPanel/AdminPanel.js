import * as Styled from "./AdminPanel.styles";

const AdminPanel = () => {
    return (
        <Styled.Wrapper>
            the admin panel for admins ..
        </Styled.Wrapper>
    );
};

export default AdminPanel;