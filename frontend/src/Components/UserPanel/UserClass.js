import * as Styled from "./UserClass.styles";

const UserClass = () => {
    return (
        <Styled.Wrapper>
            list of class for user
        </Styled.Wrapper>
    );
};

export default UserClass;